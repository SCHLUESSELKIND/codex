node_modules
dist
.DS_Store
*.local
.env
.env.*

# Laufender Sendezustand des Relays, gehört nicht ins Repo
.bop-state.json

# Prüfbilder aus OBS. Diese können den geteilten Bildschirm zeigen und damit
# Zugangsdaten oder private Inhalte. Sie dürfen nie in ein öffentliches Repo.
obs-check/
obs-*.png

# Hinweis: export/ bleibt bewusst im Repo. Das sind erzeugte Marken-Assets
# ohne Bildschirminhalte, die man direkt aus GitHub laden können soll.
